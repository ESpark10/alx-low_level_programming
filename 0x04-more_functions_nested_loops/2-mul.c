#include "main.h"

/**
 * mul - check for digit and multiply
 *
 * @a: is an integer
 *
 * @b: is an integer
 * Return: 1 if the statement is true while 0 if otherwise
 */

int mul(int a, int b)
		{
		return (a * b);
		}

